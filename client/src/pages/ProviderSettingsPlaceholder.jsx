import { DashboardPagePlaceholder } from "../components/dashboard/DashboardPagePlaceholder.jsx";

export function ProviderSettingsPlaceholder() {
  return (
    <DashboardPagePlaceholder
      description="Account, profile, notification, availability, and provider preference controls."
      moduleKey="settings"
      stageNote="Settings controls come later"
      title="Settings"
    />
  );
}
