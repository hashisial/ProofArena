import { ClientPagePlaceholder } from "../../components/client/ClientPagePlaceholder.jsx";

export function ClientSettings() {
  return (
    <ClientPagePlaceholder
      description="Manage client account, company profile, verification, notifications, billing, and preferences."
      moduleKey="settings"
      stageNote="Client account and company controls come in later settings stages."
      title="Client Settings"
    />
  );
}
