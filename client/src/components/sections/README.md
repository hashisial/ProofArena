# Legacy Empty Section Boundary

This directory is intentionally unused.

The canonical locations are:

- `client/src/sections/` for page-level composed sections.
- `client/src/components/sections/home/` for existing homepage section
  components.

Do not add new section components here. Remove this compatibility directory
after repository cleanup confirms it has no consumers.
