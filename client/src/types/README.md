# Client Contracts

The current client is JavaScript. This directory is reserved for shared
contract declarations if TypeScript or generated API contracts are introduced.

Do not place feature-specific shapes here. Those belong to their owning
feature or ProofArena module.

