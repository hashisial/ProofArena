# Stage 1.2 Duplicate Layout Audit

Generated: 2026-06-27

Audit-only. No layout was merged, renamed, or deleted. Similarity indicates overlap, not safe deletion.

## Scan Summary

- Nine files exist under `client/src/layouts/`; additional shell/header/sidebar files were scanned across `client/src/components/`.
- Provider/client layout normalized similarity: 0.766.
- Provider/client content-shell similarity: 0.981.
- Provider/client mobile-drawer similarity: 0.965.
- Provider/client topbar similarity: 0.845.
- 10 duplicate or overlap systems are recorded below.

| ID | Files | Layout/system | Purpose and users | Similarity | Material difference | Risk if retained | Risk if merged early | Confidence | Later action | Severity |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| LAY-001 | `client/src/layouts/PublicLayout.jsx`<br>`client/src/layouts/RootLayout.jsx`<br>`client/src/components/Layout.jsx` | Public shell ownership cluster | Public page chrome and root compatibility shell; AppRoutes uses PublicLayout; RootLayout is barrel-exported only; Layout is reached through RootLayout | Both compose public header/main/footer behavior, but RootLayout delegates to legacy Layout. | PublicLayout adds skip link, BackButton, Outlet, configurable chrome, and current PublicNavbar/common Footer aliases; Layout uses a local appRoutes set and direct Header/Footer. | Two public-shell policies can diverge and RootLayout ownership remains unclear. | Replacing either shell before proving all route/import behavior can change header/footer visibility. | high | Investigate; keep until RootLayout has zero runtime ownership proof. | S1 |
| LAY-002 | `client/src/components/Header.jsx`<br>`client/src/components/navigation/PublicNavbar.jsx`<br>`client/src/components/Footer.jsx`<br>`client/src/components/common/Footer.jsx` | Public navigation/footer adapter pairs | Public header/footer rendering and compatibility imports; PublicLayout imports aliases; legacy Layout imports implementations directly | PublicNavbar and common/Footer are pure re-export adapters over Header and Footer. | Adapters contain no independent rendering logic. | Two import names obscure canonical ownership and encourage new callers to choose inconsistently. | Deleting adapters without migrating PublicLayout breaks imports. | high | Keep as compatibility adapters until Stage 1.2 proves and migrates callers. | S2 |
| LAY-003 | `client/src/layouts/DashboardLayout.jsx`<br>`client/src/layouts/ClientLayout.jsx` | Provider and client dashboard shell duplication | Protected role dashboards with collapsible sidebar, topbar, drawer, and content; AppRoutes mounts both layouts | Normalized source similarity is 0.766; auth checks, grid variables, skip links, topbar/content structure, and shell state are repeated. | Role navigation, topbar copy/actions, sidebar/drawer components, and route matching differ. | Fixes to auth loading, overflow, grid transitions, and accessibility must be repeated. | An over-generic shell can erase buyer/provider role boundaries and action semantics. | high | Consolidate shared primitives later, preserve role wrappers and policy. | S1 |
| LAY-004 | `client/src/layouts/AdminLayout.jsx`<br>`client/src/layouts/DashboardLayout.jsx`<br>`client/src/layouts/ClientLayout.jsx` | Admin dashboard shell fork | Protected admin shell and shared dashboard behavior; AppRoutes mounts AdminLayout; provider/client layouts mount their roles | Admin repeats grid, auth loading, access redirects, skip link, sidebar widths, topbar/main layout. | Admin uses useUIStore aliases through useSidebarShell, embeds its mobile overlay/focus logic, and lacks a shared content-shell component. | Admin fixes can diverge from provider/client shell behavior and state semantics. | Admin security and operational UX can be weakened by a premature generic shell. | high | Investigate a tested shared shell primitive; do not merge role policy. | S1 |
| LAY-005 | `client/src/components/dashboard/DashboardContentShell.jsx`<br>`client/src/components/client/ClientContentShell.jsx`<br>`client/src/layouts/AdminLayout.jsx` | Dashboard content-container duplication | Max-width and min-width-safe dashboard content wrapping; DashboardLayout and ClientLayout import named shells; AdminLayout renders equivalent inline container | Provider/client shell similarity is 0.981 and only component/data attribute names differ. | Admin hardcodes max-w-7xl and does not expose maxWidth options. | Spacing/max-width behavior can drift across roles. | Low immediate risk, but callers may depend on data attributes or role-specific class hooks. | high | Consolidate later after browser/layout tests. | S2 |
| LAY-006 | `client/src/components/dashboard/MobileDashboardSidebar.jsx`<br>`client/src/components/client/MobileClientSidebar.jsx`<br>`client/src/layouts/AdminLayout.jsx` | Mobile dashboard drawer duplication | Overlay, Escape handling, body scroll lock, focus restore, and mobile sidebar rendering; DashboardLayout and ClientLayout import drawers; AdminLayout embeds equivalent behavior | Provider/client drawers have normalized similarity 0.965; admin repeats the lifecycle inline. | IDs, close labels, and role sidebar differ; admin state names differ. | Focus, scroll-lock, Escape, and route-close fixes can diverge. | A shared drawer must preserve unique aria-controls IDs and role content. | high | Extract only after keyboard/browser regression tests. | S2 |
| LAY-007 | `client/src/components/navigation/DashboardTopbar.jsx`<br>`client/src/components/client/ClientTopbar.jsx` | Provider/client topbar duplication | Sidebar controls, page title, CTA, notification, account menu, logout; DashboardLayout and ClientLayout | Normalized source similarity is 0.845 with repeated menu lifecycle and account helpers. | Role labels, primary CTA, account links, IDs, and copy differ. | Account menu/accessibility fixes require duplicate edits. | Generic actions could expose provider actions to clients or vice versa. | high | Share account/menu primitives later; retain role-specific action slots. | S2 |
| LAY-008 | `client/src/components/navigation/AdminTopbar.jsx`<br>`client/src/components/navigation/DashboardTopbar.jsx`<br>`client/src/components/client/ClientTopbar.jsx` | Admin topbar fork | Admin sidebar controls, search surface, notifications, account menu; AdminLayout | Normalized similarity is 0.778 against provider topbar; menu lifecycle and identity helpers repeat. | Admin uses useUIStore state and includes an uncontrolled search input with no search handler. | State/accessibility behavior diverges and the search control appears functional without workflow. | Admin-only actions and security context must remain isolated. | high | Investigate shared topbar primitives; separately classify search as placeholder. | S1 |
| LAY-009 | `client/src/components/navigation/DashboardSidebar.jsx`<br>`client/src/components/client/ClientSidebar.jsx`<br>`client/src/components/navigation/AdminSidebar.jsx`<br>`client/src/components/navigation/sidebar/SidebarCore.jsx` | Role sidebar wrapper overlap | Supply role config, brand, footer, labels, and variant to SidebarCore; All three dashboard layouts and mobile drawers | All are thin wrappers around the same SidebarCore contract. | Navigation config, brand copy, user/account footer, state source, and aria labels differ. | Some wrapper duplication is intentional; inconsistent state adapters remain possible. | Removing wrappers can push role logic into SidebarCore and make it product-specific. | high | Keep thin wrappers; consolidate only truly generic footer/brand helpers. | S3 |
| LAY-010 | `client/src/layouts/WorkspaceLayout.jsx`<br>`client/src/components/SaaSLayout.jsx`<br>`client/src/layouts/DashboardLayout.jsx` | Workspace page-shell compatibility layer | Page-level header/content composition inside dashboard routes; WorkspaceLayout is used by Saved; SaaSLayout is used directly by eleven workspace pages | WorkspaceLayout is a re-export, while SaaSLayout supplies PageHeader and content wrapper inside DashboardLayout. | It is not a replacement dashboard grid; it is an inner page shell with a hardcoded /dashboard fallback. | Direct SaaSLayout and WorkspaceLayout imports create two names for one inner-shell policy. | Treating it as a full DashboardLayout duplicate would remove required outer auth/sidebar behavior. | high | Investigate import normalization only; keep semantic distinction from DashboardLayout. | S2 |

## Negative Findings

- `client/src/layouts/AuthLayout.jsx` has no equivalent auth-shell duplicate in the registered layout set.
- Sidebar role wrappers are intentional composition over `SidebarCore`; they are not safe deletion candidates.
- `WorkspaceLayout`/`SaaSLayout` is an inner page shell, not a replacement for the protected dashboard grid.

