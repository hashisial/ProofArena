// Auth module boundary. The canonical implementation remains in the shared
// service location until the existing auth vertical is migrated as one unit.
export * from "../../services/authService.js";
